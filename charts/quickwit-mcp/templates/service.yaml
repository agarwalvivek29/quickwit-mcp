apiVersion: v1
kind: Service
metadata:
  name: {{ include "quickwit-mcp.fullname" . }}
  labels:
    {{- include "quickwit-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quickwit-mcp.selectorLabels" . | nindent 4 }}
